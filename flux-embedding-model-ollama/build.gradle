plugins {
  id 'com.gradleup.shadow' version '8.3.3'
}

dependencies {
  implementation "dev.langchain4j:langchain4j-ollama:0.35.0"
}

shadowJar {
  // Avoids conflict with Spark's OkHttp.
  relocate "okhttp3", "flux.okhttp3"
  relocate "okio", "flux.okio"
}

